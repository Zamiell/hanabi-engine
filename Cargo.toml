[workspace]
members = [
    "crates/hanabi-cli",
    "crates/hanabi-core",
    "crates/hanabi-protocol",
    "crates/hanabi-search",
]
resolver = "3"

[workspace.package]
edition = "2024"
license = "MIT"
rust-version = "1.85"

[workspace.dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[workspace.lints.rust]
dead_code = "warn"
unreachable_pub = "warn"
unsafe_code = "forbid"

[workspace.lints.clippy]
all = "warn"
pedantic = "warn"

[profile.release]
codegen-units = 1
lto = "thin"

# Convention inference is CPU-heavy even in tests. Keep debug assertions and
# overflow checks, but optimize the generated test binaries so full-game
# regression continuations do not run at `opt-level = 0`.
[profile.test]
opt-level = 2
